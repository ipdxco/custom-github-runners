name: Playground

on:
  workflow_dispatch:
  push:

defaults:
  run:
    shell: bash

jobs:
  run:
    name: Run
    runs-on: [self-hosted, linux, x64, kubo]
    steps:
      - run: aws --version
      - run: aws sts get-caller-identity
